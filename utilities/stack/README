This utility is called

> ./stack  <zipped-solution>

where <zipped-solution> is an evaluated B-spline expansion using hex-main,

> hex-main --zipfile=<solution> --zipcount=<N>

The program "stack" will create a file with two columns containing highest and lowest
elements of a corresponding row of the zipped solution.
